services:
  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    command: ["--config=/etc/otel-collector-config.yaml"]
    volumes:
      - ./otel-collector-config.yaml:/etc/otel-collector-config.yaml
    ports:
      - "4317:4317" # OTLP gRPC receiver
  jaeger:
    image: jaegertracing/all-in-one:latest
    ports:
      - "6831:6831/udp" # UDP port for Jaeger agent
      - "16686:16686" # Web UI
      - "14268:14268" # HTTP port for spans
# version: "3.8"
# services:
#   otel-collector:
#     image: otel/opentelemetry-collector:latest
#     container_name: otel-collector
#     ports:
#       - "4317:4317" # OTLP gRPC endpoint
#       - "4318:4318" # OTLP HTTP endpoint
#     command: >
#       --config=/etc/otel-collector-config.yaml
#     volumes:
#       - ./otel-collector-config.yaml:/etc/otel-collector-config.yaml
#     depends_on:
#       - jaeger
#
#   jaeger:
#     image: jaegertracing/all-in-one:latest
#     container_name: jaeger
#     ports:
#       - "16686:16686" # Jaeger UI
#       - "6831:6831/udp" # Jaeger agent (for UDP-based clients)
#       - "14250:14250" # Jaeger gRPC endpoint (for OpenTelemetry Collector)
#       - "14268:14268" # Jaeger HTTP endpoint (for trace submission)

  # Optional: Add a Prometheus instance to monitor the OpenTelemetry Collector
  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: prometheus
  #   ports:
  #     - "9090:9090"
  #   volumes:
  #     - ./prometheus.yml:/etc/prometheus/prometheus.yml
